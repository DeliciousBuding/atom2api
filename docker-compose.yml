services:
  atom2api:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ${HOME}/.atomcode:/root/.atomcode:ro
    environment:
      - PORT=8080
      - DB_PATH=/app/data/atom2api.db
      - ADMIN_SECRET=${ADMIN_SECRET:-}
    restart: unless-stopped
