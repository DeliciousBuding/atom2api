services:
  atom2api:
    build: .
    ports:
      - "${BIND_ADDR:-0.0.0.0}:${PORT:-8080}:8080"
    volumes:
      - sqlite-data:/data
    env_file:
      - .env
    environment:
      - DB_PATH=/data/atom2api.db
    restart: unless-stopped

volumes:
  sqlite-data:
